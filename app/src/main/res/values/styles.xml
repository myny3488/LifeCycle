<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar"></style>

    <style name="CreateActivityTheme" parent="Theme.AppCompat.Light.NoActionBar"></style>

    <style name="style_action_bar_text">
        <item name="android:textSize">22dp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="style_edittext">
        <item name="android:colorControlNormal">@color/light_gray</item>
        <item name="android:colorControlActivated">@color/light_gray</item>
        <item name="android:textCursorDrawable">@drawable/cursor_drawable</item>
    </style>
</resources>
